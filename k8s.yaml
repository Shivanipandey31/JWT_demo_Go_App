apiVersion: apps/v1
kind: Deployment
metadata:
  name: jwt-go-app
  namespace: demo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: jwt-go-app
  template:
    metadata:
      labels:
        app: jwt-go-app
    spec:
      containers:
        - name: jwt-go-app
          image: jwt-go-demo:v1
          imagePullPolicy: Never
          ports:
            - containerPort: 8080
          env:
            - name: PORT
              value: "8080"
---
apiVersion: v1
kind: Service
metadata:
  name: jwt-go-app
  namespace: demo
spec:
  selector:
    app: jwt-go-app
  ports:
    - port: 8080
      targetPort: 8080
  type: ClusterIP
---
apiVersion: v1
kind: Service
metadata:
  name: jwt-go-app-headless
  namespace: demo
spec:
  clusterIP: None
  selector:
    app: jwt-go-app
  ports:
    - name: http
      port: 8080
      targetPort: 8080
